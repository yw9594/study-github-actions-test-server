name: CI

on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # uses: 타 레포지토리의 actions 사용
      - uses: actions/checkout@v2 # runner 머신에서 현재 레포지토리 클론 및 체크아웃
      
      - name: Run pwd
        run: pwd

      - name: Run ls -al
        run: ls -al
